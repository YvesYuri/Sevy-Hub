import 'package:flutter/material.dart';

class DesignLibraryView extends StatelessWidget {
  const DesignLibraryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Design Library View")));
  }
}
